"""modal-workflows helpers for running on Modal: ``workflows`` and ``training``.

Import from the relevant subpackage (for example ``modal_workflows.workflows``).
This package is intentionally empty so importing one subpackage never pulls in the
heavy dependencies of another (e.g. torch via ``training``).
"""
